# Copy the contents of this file into the Streamlit Cloud Secrets panel.
# Go to: your app → Settings → Secrets → paste below (without the comment lines).
#
# This key is OPTIONAL. The app runs fully without it using the sample dataset.
# Get a free key at: https://www.alphavantage.co/support/#api-key

ALPHA_VANTAGE_API_KEY = "your_key_here"
